---
title: ��Ƭ��51ʵ�����
---
### LED以固定间隔循环闪�?```c
#include <reg52.h>
sbit led = P1^4;         
// 定义LED接在P1.4引脚

void delay(unsigned int n);

void main(void)
{
    while(1)
    {
        led = 1;           
        // 点亮LED（根据硬件，可能是低电平点亮�?        delay(65535);      
        // 延时
        led = 0;          
         // 熄灭LED
        delay(65535);      
        // 延时
    }
}

void delay(unsigned int n)
{
    while(n--);            
    // 简单循环延�?}
```

---

### 单按键开关灯(IO_test)

```c
#include <reg51.h>

sbit LEDIO = P1^4;    // LED接P1.4
sbit KEY1  = P2^0;    // 按键K1接P2.0

void delay(unsigned int DelayTime);

void main(void)
{
    while(1)
    {
        while(KEY1 == 1);   // 等待按键被按下（KEY1变为0�?        delay(500);         // 消抖延时
        if(KEY1 == 0)       // 确认确实按下（非抖动�?            LEDIO = ~LEDIO; // LED状态翻转（亮变�?灭变亮）
        while(KEY1 == 0);   // 等待按键松开
    }
}

void delay(unsigned int DelayTime)
{
    while(DelayTime--);    // 简单循环延�?}
```

修改说明

原名�?修改�?说明
LEDIO LEDIO_zjh LED引脚定义
KEY1 KEY1_zjh 按键引脚定义
delay delay_zjh 延时函数�?DelayTime DelayTime_zjh 函数形参

```c
#include <reg51.h>

sbit LEDIO_zjh = P1^4;          // LED接P1.4
sbit KEY1_zjh  = P2^0;          // 按键K1接P2.0

void delay_zjh(unsigned int DelayTime_zjh);

void main(void)
{
    while(1)
    {
        while(KEY1_zjh == 1);       // 等待按键被按�?        delay_zjh(500);             // 消抖延时
        if(KEY1_zjh == 0)           // 确认按下
            LEDIO_zjh = ~LEDIO_zjh; // LED翻转
        while(KEY1_zjh == 0);       // 等待松开
    }
}

void delay_zjh(unsigned int DelayTime_zjh)
{
    while(DelayTime_zjh--);
}
```



---
### 双按键开关灯
![[Pasted image 20260623122129.png]]

```c
#include <reg52.h>

sbit LED = P1^4;              // LED控制引脚
sbit KEY_START = P2^0;        // 开始闪烁按�?sbit KEY_STOP = P2^1;         // 停止闪烁按键

bit flag_blink = 0;           // 闪烁允许标志
unsigned char cnt = 0;        // 定时器溢出计数器

void delay(unsigned int ms)   // 延时函数（按键消抖）
{
    unsigned int i, j;
    for(i = ms; i > 0; i--)
        for(j = 110; j > 0; j--);
}

void Timer0_Init(void)        // 定时�?初始�?{
    TMOD = 0x01;              // 方式1�?6位定时器�?    TH0 = 0x4C;               // 50ms初值高8位（12MHz�?    TL0 = 0x00;               // 50ms初值低8�?    EA = 1;                   // 开总中�?    ET0 = 1;                  // 开定时�?中断
    TR0 = 1;                  // 启动定时�?}

void Timer0_ISR(void) interrupt 1   // 定时�?中断服务
{
    TH0 = 0x4C;               // 重装�?�?    TL0 = 0x00;               // 重装�?�?    cnt++;                    // 溢出计数�?
    if(cnt >= 20)             // 20�?1�?    {
        cnt = 0;
        if(flag_blink == 1)   // 允许闪烁
            LED = ~LED;       // LED翻转
    }
}

void main(void)
{
    Timer0_Init();            // 初始化定时器
    while(1)
    {
        while(KEY_START == 1); // 等待K1按下
        delay(20);             // 消抖
        if(KEY_START == 0)
        {
            flag_blink = 1;    // 开启闪�?            while(KEY_START == 0);
        }
        while(KEY_STOP == 1);  // 等待K2按下
        delay(20);             // 消抖
        if(KEY_STOP == 0)
        {
            flag_blink = 0;    // 关闭闪烁
            LED = 1;           // 熄灭LED
            while(KEY_STOP == 0);
        }
    }
}
```

### 跑马�?循环流水�?
![[Pasted image 20260623155340.png]]

```c
#include <reg51.h>
#include <intrins.h>     // 包含_crol_()循环左移函数

void mdelay(unsigned int delaytime);

void main(void)
{
    unsigned char outdata = 0x7F;  // 0111 1111，最低位(�?�?灯亮
    
    while(1)
    {
        P1 = outdata;              // 输出到P1�?        outdata = _crol_(outdata, 1); // 循环左移1位，亮灯向左移动
        mdelay(1000);              // 延时
    }
}

void mdelay(unsigned int delaytime)
{
    unsigned int j;
    for(; delaytime > 0; delaytime--)
    {
        for(j = 0; j < 125; j++)   // 内层循环�?ms�?2MHz晶振�?        {};
    }
}
```

修改对照�?
原名�?修改�?说明
mdelay mdelay_zjh 延时函数�?delaytime delaytime_zjh 函数形参
outdata outdata_zjh 流水灯数据变�?j   j_zjh 内层循环变量

```c
#include <reg51.h>
#include <intrins.h>     // 包含_crol_()循环左移函数

void mdelay_zjh(unsigned int delaytime_zjh);

void main(void)
{
    unsigned char outdata_zjh = 0x7F;  // 0111 1111，最低位(�?�?灯亮
    
    while(1)
    {
        P1 = outdata_zjh;                    // 输出到P1�?        outdata_zjh = _crol_(outdata_zjh, 1); // 循环左移1位，亮灯向左移动
        mdelay_zjh(1000);                    // 延时
    }
}

void mdelay_zjh(unsigned int delaytime_zjh)
{
    unsigned int j_zjh;
    for(; delaytime_zjh > 0; delaytime_zjh--)
    {
        for(j_zjh = 0; j_zjh < 125; j_zjh++)   // 内层循环�?ms�?2MHz晶振�?        {};
    }
}
```


---

### 花式小彩�?```c
#include <reg51.h>
#include <intrins.h>

void mdelay(unsigned int delaytime);

void main(void)
{
    unsigned char pattern = 0;
    unsigned char i;
    
    while(1)
    {
        // 花样1：从左向右流水（LED8→LED1�?        for(i = 0; i < 8; i++)
        {
            P1 = ~(0x80 >> i);      // 1000 0000 �?0100 0000 �?... �?0000 0001
            mdelay(300);
        }
        
        // 花样2：从右向左流水（LED1→LED8�?        for(i = 0; i < 8; i++)
        {
            P1 = ~(0x01 << i);      // 0000 0001 �?0000 0010 �?... �?1000 0000
            mdelay(300);
        }
        
        // 花样3：两端向中间聚拢
        P1 = 0x7E;  mdelay(400);    // 0111 1110（两端亮�?        P1 = 0x3C;  mdelay(400);    // 0011 1100
        P1 = 0x18;  mdelay(400);    // 0001 1000
        P1 = 0x00;  mdelay(400);    // 0000 0000（全亮）
        
        // 花样4：中间向两端扩散
        P1 = 0x18;  mdelay(400);    // 0001 1000
        P1 = 0x3C;  mdelay(400);    // 0011 1100
        P1 = 0x7E;  mdelay(400);    // 0111 1110
        P1 = 0xFF;  mdelay(400);    // 1111 1111（全灭）
        
        // 花样5：交替闪烁（奇偶位交替）
        for(i = 0; i < 4; i++)
        {
            P1 = 0xAA;  mdelay(200); // 1010 1010
            P1 = 0x55;  mdelay(200); // 0101 0101
        }
        
        // 花样6：逐个点亮，再逐个熄灭
        P1 = 0xFF;
        for(i = 0; i < 8; i++)
        {
            P1 &= ~(0x80 >> i);     // 从高位到低位依次点亮
            mdelay(200);
        }
        for(i = 0; i < 8; i++)
        {
            P1 |= (0x80 >> i);      // 从高位到低位依次熄灭
            mdelay(200);
        }
        
        // 花样7：全亮全灭闪�?�?        for(i = 0; i < 3; i++)
        {
            P1 = 0x00;  mdelay(300); // 全亮
            P1 = 0xFF;  mdelay(300); // 全灭
        }
        
        mdelay(500);                // 花样切换间隔
    }
}

void mdelay(unsigned int delaytime)
{
    unsigned int j;
    for(; delaytime > 0; delaytime--)
    {
        for(j = 0; j < 125; j++);
    }
}
```


---

### 定时器查询法1Hz闪灯
![[Pasted image 20260623112025.png]]
![[Pasted image 20260623112031.png]]
![[Pasted image 20260623112035.png]]

```c
#include <reg52.h>          // 包含51单片机寄存器定义头文�?
sbit led = P1^4;            // 定义LED连接的引脚为P1.4

void main(void)              // 主函�?{
    unsigned char temp = 0x00;  // 定义计数器变量，用于记录定时器溢出次�?    
    TMOD = 0x01;            // 设置定时器工作模式：定时�?，工作模�?�?6位定时器�?    TH0 = 0x4C;             // 装载定时器高8位初始值（65536-50000=15536=0x3CB0�?    TL0 = 0x00;             // 装载定时器低8位初始值（实际溢出周期�?0ms @12MHz晶振�?    TR0 = 1;                // 启动定时�?开始计�?    
    while(1)                // 主循环，无限执行
    {
        while(TF0)          // 检测定时器0溢出标志位（TF0=1表示溢出�?        {
            TH0 = 0x4C;     // 重新装载定时器高8位初始�?            TL0 = 0x00;     // 重新装载定时器低8位初始�?            TF0 = 0;        // 软件清除溢出标志位（硬件不会自动清除�?            
            temp++;         // 溢出次数�?
            
            if(temp == 20)  // 判断是否达到20次溢出（20×50ms=1000ms=1秒）
            {
                led = ~led; // LED取反，实现亮灭切�?                temp = 0;   // 计数器清零，重新开始计�?            }
        }
    }
}

```

**修改内容汇总：**

|原变量名|新变量名|说明|
|---|---|---|
|`led`|`led_zjh`|LED引脚定义|
|`temp`|`cnt_zjh`|定时器溢出计数器|
```c
#include <reg52.h>              // 包含51单片机寄存器定义头文�?
sbit led_zjh = P1^4;            // 定义LED连接的引脚为P1.4（zjh命名�?
void main(void)                  // 主函�?{
    unsigned char cnt_zjh = 0x00;   // 定义计数器变量，记录定时器溢出次数（zjh命名�?    
    TMOD = 0x01;                // 设置定时器工作模式：定时�?，模�?�?6位定时器�?    TH0 = 0x4C;                 // 装载定时器高8位初始值（50ms @12MHz�?    TL0 = 0x00;                 // 装载定时器低8位初始�?    TR0 = 1;                    // 启动定时�?开始计�?    
    while(1)                    // 主循环，无限执行
    {
        while(TF0)              // 检测定时器0溢出标志位（TF0=1表示溢出�?        {
            TH0 = 0x4C;         // 重新装载定时器高8位初始�?            TL0 = 0x00;         // 重新装载定时器低8位初始�?            TF0 = 0;            // 软件清除溢出标志�?            
            cnt_zjh++;          // 溢出次数�?（zjh命名�?            
            if(cnt_zjh == 20)   // 判断是否达到20次溢出（20×50ms=1秒）
            {
                led_zjh = ~led_zjh; // LED取反，实现亮灭切换（zjh命名�?                cnt_zjh = 0;    // 计数器清零，重新开始计�?            }
        }
    }
}
```

---

### 定时器中断法1Hz闪灯

```c
#include <reg52.h>              // 包含51单片机寄存器定义头文�?
// 本程序实�?Hz闪灯（晶振频�?1.0592MHz�?
sbit led = P1^4;                // 定义LED控制引脚为P1.4

unsigned int temp;              // 定义无符号整型变量，用于记录中断次数

void main(void)
{
    TMOD = 0x02;                // 设置定时器T0为方�?�?位自动重装载定时器）
    TH0 = 0xA4;                 // 装载重载初值：定时�?.1ms�?1.0592MHz下存�?.17%误差�?    TL0 = 0xA4;                 // 装载当前计数值（与TH0相同，方�?中TL0计数，TH0存重载值）
    TF0 = 0;                    // 清除定时器T0溢出中断标志位（初始化时确保标志�?�?    ET0 = 1;                    // 开启定时器T0中断允许（允许T0溢出时触发中断）
    EA = 1;                     // 开启系统总中断允许（使能所有中断）
    TR0 = 1;                    // 启动定时器T0开始计�?    while(1);                   // 主循环空转，等待中断发生（CPU在中断期间执行中断服务函数）
}

void timer0_int() interrupt 1   // 定时器T0中断服务函数（中断服务号�?�?{
    TF0 = 0;                    // 清除定时器T0溢出中断标志位（硬件置位后软件清除，防止重复触发�?    ET0 = 0;                    // 临时关闭定时器T0中断允许（在中断处理期间禁止嵌套中断�?    
    temp++;                     // 中断次数�?（每次中断约0.1ms�?    
    if(temp == 10000)           // 判断中断次数是否达到10000次（10000 × 0.1ms = 1000ms = 1秒）
    {
        led = ~led;             // LED状态取反，实现亮灭切换（亮→灭或灭→亮�?        temp = 0;               // 中断计数器清零，重新开始下一轮计�?    }
    
    ET0 = 1;                    // 重新开启定时器T0中断允许（中断处理完成后恢复中断响应�?}
```

 变量名修改对照表

| 原变量名         | 新变量名（含姓名拼音zjh�?  | 说明                   |
| ------------ | ---------------- | -------------------- |
| `led`        | `led_zjh`        | LED引脚定义              |
| `temp`       | `cnt_zjh`        | 中断次数计数器，cnt = count�?|
| `timer0_int` | `timer0_int_zjh` | 定时器中断服务函数名           |

```c
#include <reg52.h>

sbit led_zjh = P1^4;

unsigned int cnt_zjh;

void main(void)
{
    TMOD = 0x02;
    TH0 = 0xA4;
    TL0 = 0xA4;
    TF0 = 0;
    ET0 = 1;
    EA = 1;
    TR0 = 1;
    while(1);
}

void timer0_int_zjh() interrupt 1
{
    TF0 = 0;
    ET0 = 0;
    cnt_zjh++;
    if(cnt_zjh == 10000)
    {
        led_zjh = ~led_zjh;
        cnt_zjh = 0;
    }
    ET0 = 1;
}
```

---

### 单一中断
```c
#include<reg52.h>          // 包含51单片机寄存器定义头文�?
sbit led=P1^4;             // 定义LED控制引脚为P1.4（开发板上低电平点亮LED�?
void delay(unsigned int n); // 声明延时函数

void main(void)
{
    IT0=1;                 // 设置外部中断0触发方式为下降沿触发（按键按下瞬间，电平从高变低�?    EX0=1;                 // 使能外部中断0（允许INT0中断请求�?    EA=1;                  // 开启总中断（CPU允许所有中断）

    while(1)               // 主循环，程序持续运行
    {
        led=1;             // LED熄灭（P1.4输出高电平）
        delay(65535);      // 延时�?.5秒（由晶振频�?1.0592MHz决定�?        led=0;             // LED点亮（P1.4输出低电平）
        delay(65535);      // 延时�?.5�?    }                      // 循环执行，LED持续闪烁
}

/*
 * 函数名：int0_int
 * 功能：外部中�?的中断服务函�? * 触发条件：P3.2引脚检测到下降沿（按键按下�? * 中断编号：interrupt 0 表示外部中断0
 */
void int0_int() interrupt 0
{
    unsigned char i;       // 定义循环变量，用于控制延时次�?    
    IE0=0;                 // 手动清除外部中断0的中断标志位（防止退出中断后再次进入�?    EX0=0;                 // 暂时关闭外部中断0（防止中断嵌套或重复触发，保证中断处理完整性）
    
    led=0;                 // LED点亮（低电平有效�?    
    for(i=0;i<50;i++)      // 循环50次，每次延时�?.05秒，总共延时�?.5�?    {
        delay(65535);      // 调用延时函数，每次约0.05秒（实际�?.5秒�?0�?2.5秒）
    }                      // 此期间LED保持点亮状�?    
    EX0=1;                 // 重新开启外部中�?（中断处理完成，允许再次响应中断�?}

/*
 * 函数名：delay
 * 功能：简单的软件延时
 * 参数：n - 延时循环次数，n越大延时越长
 * 原理：通过CPU执行空循环消耗时�? */
void delay(unsigned int n)
{
    while(n--);            // n次递减循环，每次循环消耗若干机器周�?}
```

```c
#include<reg52.h>
sbit zjh_led=P1^4;
void zjh_delay(unsigned int n);
void main(void)
{
    IT0=1;
    EX0=1;
    EA=1;
    while(1)
    {
        zjh_led=1;
        zjh_delay(65535);
        zjh_led=0;
        zjh_delay(65535);
    }
}
void zjh_int0_int() interrupt 0
{
    unsigned char i;
    IE0=0;
    EX0=0;
    zjh_led=0;
    for(i=0;i<50;i++)
    {
        zjh_delay(65535);
    }
    EX0=1;
}
void zjh_delay(unsigned int n)
{
    while(n--);
}
```

---

### 中断的嵌�?```c
#include<reg52.h>          // 包含51单片机寄存器定义头文�?
sbit led=P1^4;             // 定义LED控制引脚为P1.4（低电平点亮�?
void delay(unsigned int n); // 声明延时函数

void main(void)
{
    // ===== 外部中断0配置 =====
    IT0=1;                 // 设置外部中断0为下降沿触发（按键按下瞬间触发）
    EX0=1;                 // 使能外部中断0
    PX0=1;                 // 设置INT0为高优先级（高于INT1�?    
    // ===== 外部中断1配置 =====
    IT1=1;                 // 设置外部中断1为下降沿触发（按键按下瞬间触发）
    EX1=1;                 // 使能外部中断1
    PX1=0;                 // 设置INT1为低优先级（低于INT0�?    
    EA=1;                  // 开启总中�?
    while(1)               // 主循环：LED持续闪烁
    {
        led=1;             // LED熄灭（高电平�?        delay(65535);      // 延时�?.5�?        led=0;             // LED点亮（低电平�?        delay(65535);      // 延时�?.5�?    }
}

// ===== 外部中断0服务函数（高优先级） =====
void int0_int() interrupt 0
{
    unsigned char i;       // 定义循环变量
    
    IE0=0;                 // 手动清除外部中断0中断标志�?    EX0=0;                 // 关闭INT0中断（防止嵌�?重复触发�?    led=0;                 // LED点亮（低电平有效�?    
    for(i=0;i<50;i++)      // 循环50次，延时�?.5�?    {
        delay(65535);      // 每次延时�?.5�?    }
    
    EX0=1;                 // 重新开启INT0中断
}

// ===== 外部中断1服务函数（低优先级） =====
void int1_int() interrupt 2
{
    unsigned char i;       // 定义循环变量
    
    IE1=0;                 // 手动清除外部中断1中断标志�?    EX1=0;                 // 关闭INT1中断（防止嵌�?重复触发�?    led=1;                 // LED熄灭（高电平有效�?    
    for(i=0;i<50;i++)      // 循环50次，延时�?.5�?    {
        delay(65535);      // 每次延时�?.5�?    }
    
    EX1=1;                 // 重新开启INT1中断
}

// ===== 延时函数 =====
void delay(unsigned int n)
{
    while(n--);            // 循环n次，消耗时间实现延�?}
```

```c
#include<reg52.h>
sbit zjh_led=P1^4;
void zjh_delay(unsigned int n);
void main(void)
{
    IT0=1;
    IT1=1;
    EX0=1;
    EX1=1;
    PX0=1;
    PX1=0;
    EA=1;
    while(1)
    {
        zjh_led=1;
        zjh_delay(65535);
        zjh_led=0;
        zjh_delay(65535);
    }
}
void zjh_int0_int() interrupt 0
{
    unsigned char i;
    IE0=0;
    EX0=0;
    zjh_led=0;
    for(i=0;i<50;i++)
    {
        zjh_delay(65535);
    }
    EX0=1;
}
void zjh_int1_int() interrupt 2
{
    unsigned char i;
    IE1=0;
    EX1=0;
    zjh_led=1;
    for(i=0;i<50;i++)
    {
        zjh_delay(65535);
    }
    EX1=1;
}
void zjh_delay(unsigned int n)
{
    while(n--);
}
```

**交换优先�?*

```c
#include<reg52.h>

sbit led=P1^4;

void delay(unsigned int n);

void main(void)
{
    IT0=1; //int0 下降沿触�?	  IT1=1; //int1 下降沿触�?    EX0=1; //enable int0 int
	  EX1=1; //enable int1 int
	
	  /*在这强制修改INT0、INT1的优先级*/
	  PX0=0; //int0低优先级
	  PX1=1; //int1高优先级 
	
    EA=1;  //enable global int

    while(1)
    {
    led=1;
    delay(65535);
    led=0;
    delay(65535);
    }
}

void int0_int() interrupt 0
{
    unsigned char i;
    IE0=0; //clear int0 int flag
    EX0=0; //disable int0 int
    led=0; //低电平时LED点亮
    for(i=0;i<50;i++)
    {
        delay(65535);
    }
    EX0=1; //enable int0 int
}

void int1_int() interrupt 2
{
    unsigned char i;
    IE1=0; //clear int0 int flag
    EX1=0; //disable int0 int
    led=1; //高电平时LED熄灭
    for(i=0;i<50;i++)
    {
        delay(65535);
    }
    EX1=1; //enable int0 int
}

void delay(unsigned int n)
{
    while(n--);
}

```

### 中断的嵌套仿真版
```c
#include<reg52.h>
sbit led=P1^4;
void delay(unsigned int n);

void main(void)
{
    IT0=1;      // 下降沿触�?    EX0=1;      // 使能INT0
    PX0=1;      // INT0高优先级
    IT1=1;
    EX1=1;
    PX1=0;      // INT1低优先级
    EA=1;
    while(1)
    {
        led=1;
        delay(65535);
        led=0;
        delay(65535);
    }
}

void int0_int() interrupt 0
{
    unsigned char i;
    // 注意：硬件自动清除IE0，不需要手动写IE0=0
    // EX0不要关，否则高优先级无法体现打断效果
    led=0;                  // 进入INT0，LED熄灭
    for(i=0;i<5;i++)        // 缩短延时，避免卡太久
    {
        delay(10000);
    }
    // 退出后硬件自动恢复，不需要重新开EX0
}

void int1_int() interrupt 2
{
    unsigned char i;
    // IE1硬件自动清除
    led=1;                  // 进入INT1，LED点亮
    for(i=0;i<5;i++)
    {
        delay(10000);
    }
}

void delay(unsigned int n)
{
    while(n--);
}
```

---

### 双定时计数器协同

**�?次K1后，LED开始以500ms周期持续闪烁（亮500ms→灭500ms→亮500ms→灭500ms……）**�?
==电路图存�?=
![[Pasted image 20260630150118.png]]

```c
#include <reg51.h>                // 包含51单片机寄存器定义头文�?
bit led_flag = 0;                 // LED状态标志：0=亮，1=�?unsigned int count = 0;           // 用于500ms定时的计数器（每50ms�?，到10即为500ms�?
/*******************************************************************************
 * 函数名：T0_ISR
 * 功能�? T0定时器中断服务函数，�?0ms自动调用一�? * 中断号：1（定时器0�? ******************************************************************************/
void T0_ISR() interrupt 1
{
    // 方式1�?6位定时器，溢出后TH0/TL0自动归零，不会自动重装初�?    // 所以每次进入中断都要手动重装，保证下次仍然�?0ms
    TH0 = (65536 - 50000) / 256;  // 重装�?位初�?    TL0 = (65536 - 50000) % 256;  // 重装�?位初�?
    count++;                       // 50ms计时累加
    if(count >= 10)               // 50ms × 10 = 500ms，时间到
    {
        count = 0;                // 计数器清零，重新开始下一�?00ms计时
        led_flag = !led_flag;     // 翻转LED状态标志（0�? �?1�?�?        
        if(led_flag)              // 根据标志位控制LED
            P1 = 0xFF;            // led_flag=1 �?全灭（P1输出高电平）
        else
            P1 = 0x00;            // led_flag=0 �?全亮（P1输出低电平）
    }
}

/*******************************************************************************
 * 函数名：T1_ISR
 * 功能�? T1计数器中断服务函数，每按4次K1自动调用一�? * 中断号：3（定时器1�? * 工作方式：方�?�?位自动重装），初�?0xFC�?56-4=252�? ******************************************************************************/
void T1_ISR() interrupt 3
{
    // 方式2为自动重装模式，溢出后TH1的值自动装入TL1
    // 所以这里不需要手动重装初值，硬件自动完成

    TR0 = 1;                      // 启动T0定时器（开�?0ms计时�?    ET0 = 1;                      // 开启T0中断允许（允许T0触发中断�?}

/*******************************************************************************
 * 函数名：main
 * 功能�? 主函数，完成所有初始化配置后进入死循环
 ******************************************************************************/
void main()
{
    // ========== 1. 配置定时�?计数器工作模�?==========
    TMOD = 0x61;
    // �?�?0110 �?T1工作方式2�?位自动重装），计数器模式（C/T=1�?    // �?�?0001 �?T0工作方式1�?6位定时器），定时器模式（C/T=0�?    // 二进制：0110 0001 = 0x61

    // ========== 2. 设置初�?==========
    TH1 = 0xFC;                   // T1初值高8位：256 - 4 = 252 = 0xFC
    TL1 = 0xFC;                   // T1初值低8位（方式2下TL1是实际计数器�?
    TH0 = (65536 - 50000) / 256;  // T0�?位初值：15536 / 256 = 60
    TL0 = (65536 - 50000) % 256;  // T0�?位初值：15536 % 256 = 176

    // ========== 3. I/O端口初始�?==========
    P1 = 0x00;                    // LED初始状态：全亮（低电平有效�?
    // ========== 4. 中断系统配置 ==========
    EA = 1;                       // 开启总中断允许（总开关）
    ET1 = 1;                      // 开启T1中断允许（按4次K1触发�?    // 注意：ET0暂不开启，等T1触发后再由T1中断服务函数开�?
    // ========== 5. 启动定时�?计数�?==========
    TR1 = 1;                      // 启动T1计数器（开始对P3.5外部脉冲计数�?    // 注意：T0暂不启动，等T1触发后再由T1中断服务函数启动

    // ========== 6. 主循�?==========
    while(1)                      // 死循环，所有工作由中断服务函数完成
    {
        // CPU在此处空转，等待中断发生
        // 中断触发后自动跳转到对应的中断服务函数执�?    }
}
```

---

### 串行通信中断子程�?
功能要求：在通常情况下，单片机在执行一个闪灯的程序，这个程序让LED指示灯不停闪烁。为串行通信编写一个中断服务子程序，当单片机接收到从PC机发送来的字符时，立即把这个字符填写到SBUF寄存器，然后原封不动地发还回去。（晶振频率�?1.0592MHz，波特率要求�?600bps，采用中断方式编程）

```c
#include "reg52.h"                 // 包含52系列单片机寄存器定义头文�?
sbit led = P1^4;                  // 定义LED控制位：P1.4引脚

// 定义初始化成功提示字符串（存储在程序存储器ROM中，节省RAM空间�?unsigned char code table[] = "串口初始化成功！�?゜▽�?\n";

// ==================== 函数声明 ====================
void delay(unsigned int n);                // 延时函数声明
void UART_Mode1_Init(void);               // 串口方式1初始化函数声�?void Send_Char(unsigned char temp);       // 发送单个字符函数声�?void Send_String(unsigned char *temp);    // 发送字符串函数声明

/*******************************************************************************
 * 函数名：main
 * 功能�? 主函�? * 说明�? 1. 初始化串�? *         2. 发送提示字符串
 *         3. LED以约0.5秒周期闪烁（晶振12MHz时）
 *         4. 串口中断接收数据并回�? ******************************************************************************/
void main(void)
{
    UART_Mode1_Init();            // 调用串口初始化函数（设置SCON、TMOD、TH1等）
    Send_String(table);           // 发送提示字符串到PC端显�?    ES = 1;                       // 开启串口中断（允许接收中断�?
    while(1)                      // 主循环：LED闪烁
    {
        led = 1;                  // 点亮LED（低电平点亮，此处根据实际电路调整）
        delay(65535);             // 延时�?.5秒（12MHz晶振�?        led = 0;                  // 熄灭LED
        delay(65535);             // 延时�?.5�?    }
}

/*******************************************************************************
 * 函数名：uart_int
 * 功能�? 串口中断服务函数
 * 中断号：4（串行口中断�? * 说明�? 接收到数据后，原封不动地回传（回显功能）
 ******************************************************************************/
void uart_int(void) interrupt 4
{
    unsigned char temp;           // 定义临时变量，存放接收到的数�?
    temp = SBUF;                  // 读取SBUF，获取接收到的字�?    RI = 0;                       // 软件清零接收中断标志（硬件置1，必须软件清零）

    Send_Char(temp);              // 调用发送函数，将收到的字符立即发送回去（回显�?}

/*******************************************************************************
 * 函数名：delay
 * 功能�? 软件延时函数
 * 参数�? n - 延时循环次数（延时时�?= n × �?个机器周期）
 * 说明�? 简单空循环延时，精度不高，适用于毫秒级延时
 ******************************************************************************/
void delay(unsigned int n)
{
    while(n--);                   // 循环n次，每次循环消耗约2个机器周�?}

/*******************************************************************************
 * 函数名：UART_Mode1_Init
 * 功能�? 串口方式1初始化（8位UART，波特率9600�? * 说明�? 1. 串口工作方式1�?0位帧�?起始 + 8数据 + 1停止�? *         2. 允许接收（REN=1�? *         3. T1方式2�?位自动重装）作为波特率发生器
 *         4. 晶振11.0592MHz，波特率9600，初�?xFD
 *         5. 开启总中断（EA=1�? *         6. 启动T1定时器（TR1=1�? ******************************************************************************/
void UART_Mode1_Init()
{
    // ===== 1. 辅助寄存器设�?=====
    AUXR = 0x00;                  // 定时器时�?2分频（传�?1模式�?
    // ===== 2. 串口控制寄存器SCON设置 =====
    SM0 = 0;                      // SM0=0，SM1=1 �?工作方式1�?0位异步收发）
    SM1 = 1;                      // （SM0、SM1位于SCON最高两位）
    REN = 1;                      // 允许串口接收数据
    TI = 0;                       // 发送中断标志清零（初始状态）
    RI = 0;                       // 接收中断标志清零（初始状态）

    // ===== 3. 定时器T1配置 =====
    TMOD = 0x20;                  // T1工作方式2�?位自动重装定时器�?                                  // �?�?0010：GATE=0，C/T=0（定时），M1=1，M0=0

    // ===== 4. 波特率加倍控�?=====
    PCON &= 0x7F;                 // SMOD=0（波特率不加倍）
                                  // PCON最高位为SMOD�?0x7F将其清零

    // ===== 5. 设置波特率初�?=====
    TH1 = 0xFD;                   // 初值高8位：253�?600bps @ 11.0592MHz�?    TL1 = 0xFD;                   // 初值低8位：253（方�?下TL1为实际计数器�?
    // ===== 6. 中断系统配置 =====
    EA = 1;                       // 开启总中断允�?
    // ===== 7. 启动定时�?=====
    TR1 = 1;                      // 启动T1定时器（开始计数）
}

/*******************************************************************************
 * 函数名：Send_Char
 * 功能�? 发送单个字符（阻塞方式�? * 参数�? temp - 要发送的字符
 * 说明�? 1. 将数据写入SBUF启动发�? *         2. 等待TI标志变为1（发送完成）
 *         3. 软件清零TI标志
 ******************************************************************************/
void Send_Char(unsigned char temp)
{
    SBUF = temp;                  // 将数据写入SBUF，硬件自动启动发�?
    while(!TI);                   // 等待发送完成（TI由硬件置1�?                                  // !TI为真时循环等待，TI=1时退出循�?
    TI = 0;                       // 软件清零发送中断标志（准备下一次发送）
}

/*******************************************************************************
 * 函数名：Send_String
 * 功能�? 发送字符串（以'\0'结尾�? * 参数�? temp - 指向字符串首地址的指�? * 说明�? 逐个字符调用Send_Char发送，直到遇到结束�?\0'停止
 ******************************************************************************/
void Send_String(unsigned char *temp)   // temp指向字符串首地址
{
    while(*temp != '\0')          // 判断当前字符是否为字符串结束�?\0'
    {
        SBUF = *temp;             // 将当前字符写入SBUF，启动发�?        while(!TI);               // 等待发送完�?        TI = 0;                   // 清零发送中断标�?        temp++;                   // 指针指向下一个字�?    }
}
```

---

### RS232串口通信封装

**main.c**
```c
#include "uart.h"
sbit led = P1^4;
unsigned char code table[] = "HELLOworld";
void delay(unsigned int n) { while(n--); }
void main(void)
{
    UART_Init();
    UART_SendString(table);
    ES = 1;
    while(1) {
        led = 1; delay(65535);
        led = 0; delay(65535);
    }

}
void uart_int(void) interrupt 4
{
    unsigned char temp = SBUF;
    RI = 0;
    UART_SendChar(temp);
}
```


**uart.c**

```c
#include "uart.h"
void UART_Init(void)
{
    SM0 = 0; SM1 = 1;        // 方式1: 10位异步收�?    REN = 1; TI = 0; RI = 0; // 允许接收, 清标�?    TMOD = 0x20;             // T1方式2, 8位自动重�?    PCON &= 0x7F;            // SMOD=0
    TH1 = BAUD_9600;
    TL1 = BAUD_9600;
    EA = 1; TR1 = 1;         // 开中断, 启动T1
}
void UART_SendChar(unsigned char ch)
{
    SBUF = ch;
    while(!TI);
    TI = 0;
}
void UART_SendString(unsigned char *str)
{
    while(*str) UART_SendChar(*str++);
}
```


**uart.h**

```c
#ifndef __UART_H__
#define __UART_H__
#include "reg52.h"
#define BAUD_9600  0xFD  // 9600bps @ 11.0592MHz
void UART_Init(void);
void UART_SendChar(unsigned char ch);
void UART_SendString(unsigned char *str);
#endif
```

---

### AD转换中断

```c
#include <reg51.h>
#include <intrins.h>

/* ---------- 手动定义STC12C5A60S2特殊功能寄存�?---------- */
sfr P1ASF    = 0x9D;    // P1口模拟功能控�?sfr ADC_CONTR = 0xBC;   // ADC控制寄存�?sfr ADC_RES   = 0xBD;   // ADC结果�?�?sfr ADC_RESL  = 0xBE;   // ADC结果�?�?sfr AUXR1    = 0xA2;    // 辅助寄存�?
sfr IPH      = 0xB7;    // 中断优先级高字节

/* ---------- 常量定义 ---------- */
#define ADC_POWER  0x80  // ADC电源
#define ADC_FLAG   0x10  // 转换完成标志
#define ADC_START  0x08  // 启动转换
#define ADC_SPEED  0x60  // 速度�?40时钟周期

#define EADC       0x20  // ADC中断允许位（IE.5�?#define ADRJ       0x04  // 数据格式控制位（AUXR1.2�?
/* ---------- 位定�?---------- */
sbit A0 = P2^7;
sbit A1 = P2^6;
sbit A2 = P2^5;
sbit A3 = P2^4;
sbit sda = P1^5;
sbit clk = P1^7;

unsigned char code DisTable[10] = {
    0x3f, 0x06, 0x5b, 0x4f, 0x66,
    0x6d, 0x7d, 0x07, 0x7f, 0x6f
};

/* ---------- 函数声明 ---------- */
void delay(unsigned int n);
void SPISend(unsigned char num);
void display(unsigned int dat);
void ADC_Init(void);

/* ---------- 全局变量 ---------- */
unsigned int ADRES = 0;
bit ADC_Complete = 0;

/* =====================================================
   主函�?   ===================================================== */
void main(void)
{
    ADC_Init();                 // ADC初始�?    EA = 1;                     // 开启总中�?    IE |= EADC;                 // 【重点】开启ADC中断（IE.5�?�?    
    while(1)
    {
        if(ADC_Complete)
        {
            ADC_Complete = 0;
            ADRES = ADRES * 5000 / 1024;  // 转换为电�?mV)
            display(ADRES);
        }
        ADC_CONTR = ADC_POWER | ADC_START | 0x01;  // 启动ADC，通道1
        delay(100);
    }
}

/* =====================================================
   【重点】ADC中断服务函数，中断号=5
   ===================================================== */
void ADC_ISR(void) interrupt 5
{
    ADC_CONTR &= ~ADC_FLAG;     // 软件清除转换完成标志
    ADRES = ADC_RES;            // 读取�?�?    ADRES = (ADRES << 8) | ADC_RESL;  // 合成10位结�?    ADC_Complete = 1;
}

/* =====================================================
   ADC初始化函�?   ===================================================== */
void ADC_Init(void)
{
    P1ASF = 0x02;               // P1.1为模拟输�?    ADC_RES = 0;
    ADC_RESL = 0;
    ADC_CONTR = ADC_POWER | 0x01;  // 打开ADC电源，选择通道1
    AUXR1 |= ADRJ;              // ADRJ=1，调整数据格�?    delay(1000);
}

/* =====================================================
   显示函数（显示电压值，带小数点�?   ===================================================== */
void display(unsigned int dat)
{
    A0 = 1; A1 = 1; A2 = 1; A3 = 1;
    
    // 千位（带小数点）
    SPISend(DisTable[dat/1000] | 0x80);
    A3 = 0;
    delay(500);
    A3 = 1;
    
    // 百位
    SPISend(DisTable[dat%1000/100]);
    A2 = 0;
    delay(500);
    A2 = 1;
    
    // 十位
    SPISend(DisTable[dat%100/10]);
    A1 = 0;
    delay(500);
    A1 = 1;
    
    // 个位
    SPISend(DisTable[dat%10]);
    A0 = 0;
    delay(500);
    A0 = 1;
}

/* =====================================================
   SPI发送函数（模拟SPI时序�?   ===================================================== */
void SPISend(unsigned char num)
{
    unsigned char temp;
    clk = 0;
    sda = 0;
    for(temp = 8; temp > 0; temp--)
    {
        num <<= 1;      // 左移，最高位移到CY
        sda = CY;       // CY中为最高位
        _nop_();
        clk = 1;        // 上升沿发送数�?        _nop_();
        clk = 0;
    }
}

/* =====================================================
   延时函数
   ===================================================== */
void delay(unsigned int n)
{
    while(n--);
}
```


